import { ChatplaceDirective } from './chatplace.directive';

describe('ChatplaceDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatplaceDirective();
    expect(directive).toBeTruthy();
  });
});
